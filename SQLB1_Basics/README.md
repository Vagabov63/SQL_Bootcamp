# Общая суть проекта

В данном проекте реализован комплекс SQL-запросов для работы с реляционной базой данных сети пиццерий. База данных включает пять связанных таблиц: справочники пиццерий, клиентов и меню, а также журналы посещений и заказов.

Основная цель — отработать ключевые навыки работы с SQL:
- фильтрацию данных по различным условиям (города, даты, диапазоны значений);
- сортировку с разными направлениями по нескольким полям;
- создание вычисляемых полей с условной логикой (CASE);
- использование вложенных подзапросов в секциях SELECT и FROM;
- формирование строковых значений по заданному шаблону;
- работу с уникальными значениями и чётными идентификаторами.

Все запросы выполнены без использования JOIN там, где это требовалось условиями, что позволило глубже освоить альтернативные подходы к получению связанных данных через подзапросы. Результаты охватывают широкий спектр типовых задач анализа данных — от простых выборок до многоуровневых вложенных конструкций.

--- 

# Выполненные задания

### Exercise 00 — Базовая выборка по городу
Выбрал имена и возраст всех людей, которые живут в городе Казань. Простая фильтрация строк с помощью `WHERE`.

---

### Exercise 01 — Фильтрация по полу и сортировка
Выбрал имена и возраст только женщин из Казани и отсортировал результат по имени в алфавитном порядке. Добавил условие по полу и сортировку.

---

### Exercise 02 — Рейтинг пиццерий (два способа) 
Выбрал названия и рейтинги пиццерий с рейтингом от 3.5 до 5 включительно. Реализовал два синтаксически разных варианта:
- через операторы сравнения `>=` и `<=`
- через ключевое слово `BETWEEN`.

Результат отсортировал по рейтингу.

---

### Exercise 03 — Уникальные посетители за период
Нашёл уникальные идентификаторы людей (DISTINCT person_id), которые либо посещали пиццерии с 6 по 9 января 2022 года, либо посещали пиццерию с ID = 2. Результат отсортировал по убыванию ID.

---

### Exercise 04 — Формирование строки с данными о человеке
Создал вычисляемое поле `person_information`, которое объединяет ***имя***, ***возраст***, ***пол*** и ***адрес*** в одну строку.
Использовал конкатенацию с правильным экранированием кавычек. Отсортировал результат по этому вычисляемому полю.

---

### Exercise 05 — Имена людей через подзапрос в SELECT
Через вложенный подзапрос в секции `SELECT` получил имена людей, которые сделали заказы с ID меню 13, 14 или 18 в дату 7 января 2022 года. Обошёлся без `JOIN` и `IN`.

---

### Exercise 06 — Добавление проверки на имя Denis
К предыдущему запросу добавил новую вычисляемую колонку `check_name`, которая возвращает `true`, если человек — Денис, и `false` в противном случае. Реализовал через `CASE` в подзапросе.

---

### Exercise 07 — Возрастные интервалы
Для каждого человека создал категорию возраста (interval_info):
- `interval #1` — от 10 до 20 лет
- `interval #2` — от 21 до 23 лет,
- `interval #3` — все остальные.

Использовал `CASE` с `BETWEEN`, отсортировал результат по имени интервала.

---

### Exercise 08 — Чётные идентификаторы заказов
Выбрал все записи из таблицы заказов, у которых `ID` чётный (проверял через `% 2 = 0`), и отсортировал результат по ID. Отработал работу с арифметическими операциями в условиях.

---

### Exercise 09 — Имена и пиццерии через вложенный FROM
Получил пары «имя человека — название пиццерии» для визитов с 7 по 9 января 2022 года. Использовал:
- подзапрос в `FROM` для фильтрации визитов по дате,
- вложенные подзапросы в `SELECT` для подстановки имени и названия из справочников.

Отсортировал по имени по возрастанию, по названию пиццерии — по убыванию. Обошёлся без `JOIN`.